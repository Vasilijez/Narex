[build-system]
requires = ["flit_core >=3.11,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "narex"
version = "0.1.0"
dependencies = [
    "textX",
    "click",
]

[project.scripts]
narex = "narex.cli.main:cli"