# Directory content clarification
- `grammar.tx` is a grammar file provided for checking the rules by extension.
- `extension.ts` is a file used for running an extension client.
- `server.py` is a file which comprehends a logic of running a LSP server checking Narex grammar, and handling other extension features.